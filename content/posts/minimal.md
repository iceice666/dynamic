This is the simplest possible post — no frontmatter at all.

The date is inferred from the first git commit of this file.
Tags are inferred from the last line if it matches `#tag #tag` syntax.

Both `publishedAt` and `tags` are fully optional.
