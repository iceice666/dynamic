# Local development environment variables
#
# Copy to .env for `astro dev`, or to .dev.vars for `wrangler dev`.
# In production, set these as Cloudflare Worker Secrets in the dashboard
# (Workers & Pages → your worker → Settings → Variables and Secrets).
#
# `wrangler dev` reads .dev.vars and exposes these as runtime bindings,
# which is the recommended local workflow for the Cloudflare adapter.

# Base URL of your Umami instance (no trailing slash)
UMAMI_API_URL=https://analytics.example.com

# Website ID from Umami dashboard → Settings → Websites
UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# --- Authentication (choose one) ---

# API key (Umami Cloud or instances with API key auth enabled)
UMAMI_API_KEY=

# Username + password (self-hosted instances without API key auth)
UMAMI_USERNAME=
UMAMI_PASSWORD=

# --- Optional ---

# Custom URL for the tracking script (defaults to ${UMAMI_API_URL}/script.js)
UMAMI_SCRIPT_URL=
